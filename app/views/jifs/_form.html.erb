<%= form_for(@jif) do |f| %>
  <% if @jif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jif.errors.count, "error") %> prohibited this jif from being saved:</h2>

      <ul>
      <% @jif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
